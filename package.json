{
  "name": "conductor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Universal task runner & dashboard for developers",
  "license": "MIT",
  "author": {
    "name": "PhantomDave",
    "email": "davide.rodo@pdave.dev"
  },
  "homepage": "https://github.com/PhantomDave/conductor",
  "repository": {
    "type": "git",
    "url": "https://github.com/PhantomDave/conductor.git"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:core": "bun run --cwd packages/core dev",
    "dev:ui": "bun run --cwd packages/ui dev",
    "dev:cli": "bun run --cwd packages/cli dev",
    "dev:desktop": "bun run --cwd packages/core build:sidecar && bun run --cwd packages/ui build && bun run --cwd packages/desktop dev",
    "link:cli": "bun run --cwd packages/cli link",
    "build": "bun run --cwd packages/core build && bun run --cwd packages/cli build && bun run --cwd packages/ui build",
    "build:desktop": "bun run --cwd packages/core build:sidecar && bun run --cwd packages/ui build && bun run --cwd packages/desktop dist",
    "test": "bun test",
    "lint": "eslint . --ext .ts,.tsx",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "bun run --cwd packages/core typecheck && bun run --cwd packages/cli typecheck && bun run --cwd packages/ui typecheck && bun run --cwd packages/desktop typecheck"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "@eslint/js": "^10.0.1",
    "eslint": "^10.7.0",
    "typescript-eslint": "^8.16.0",
    "prettier": "^3.3.3",
    "typescript": "^7.0.2"
  }
}
