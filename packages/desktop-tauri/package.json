{
  "name": "@conductor/desktop-tauri",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "stage-sidecar": "node scripts/stage-sidecar.mjs",
    "dev": "bun run stage-sidecar && tauri dev",
    "build": "bun run stage-sidecar && tauri build",
    "icon": "tauri icon ../desktop/build/icon.png"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
